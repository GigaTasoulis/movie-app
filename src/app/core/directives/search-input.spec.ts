import { SearchInput } from './search-input';

describe('SearchInput', () => {
  it('should create an instance', () => {
    const directive = new SearchInput();
    expect(directive).toBeTruthy();
  });
});
